<%- include('partials/sidebar', { regions, region, parameter }) %>

<div class="flex-1 p-8 overflow-y-auto">
    <h1 class="text-4xl font-bold mb-4">Ocean Data Visualization</h1>
    <p class="text-gray-600 mb-8">
        Select a region and a parameter to view the data.
    </p>

    <!-- Here we correctly pass the variables to the chart partial -->
    <%- include('partials/chart', { chartData, region, parameter }) %>

</div>
